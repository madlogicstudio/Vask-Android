import { Text, View } from 'react-native'

export default function DashboardTab() {
    return (
        <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
            <Text style={{fontSize: 16}}>Dashboard Tab</Text>

        </View>
    )
}